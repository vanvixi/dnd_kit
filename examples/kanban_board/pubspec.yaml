name: kanban_board
description: Kanban board showcase for the dnd_kit Flutter drag-and-drop toolkit.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

resolution: workspace

dependencies:
  dnd_kit:
    path: ../../packages/dnd_kit

  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
