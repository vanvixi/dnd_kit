# Harness durable layer — each project instance generates its own data.
harness.db
harness.db-wal
harness.db-shm

# Rust build output for harness tooling.
target/
dist/

# Downloaded Harness CLI binary for installed project instances.
scripts/bin/harness-cli
scripts/bin/harness-cli.exe

# Dart and Flutter generated workspace state.
.dart_tool/
.packages
pubspec.lock
!/pubspec.lock
pubspec_overrides.yaml
**/pubspec_overrides.yaml

# Editor files generated by Melos or IDEs.
.idea/
*.iml

# FVM Version Cache
.fvm/
